spring.application.name=CRUD-Operation-Spring-Techie
spring.datasource.url = jdbc:mysql://localhost:3306/employeedb?useSSL=true&requireSSL=true
spring.datasource.username = root
spring.datasource.password = Raja@0904

# JPA Settings
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# actuator
management.endpoints.web.exposure.include = *
management.endpoints.web.exposure.exclude = metrics
management.endpoint.shutdown.enabled = true